\documentclass[12pt,titlepage]{article}
  \usepackage[T1]{fontenc}
  \usepackage[latin1]{inputenc}
  \usepackage[francais]{babel}
  \usepackage{lucidabr}
  \usepackage{booktabs,tabularx}

  \title{Correspondance entre les noms de lois dans \emph{Loss Models}
    et dans \texttt{actuar}}
  \author{Vincent Goulet}

\begin{document}

\maketitle

\section*{Famille bêta transformée}

\begin{center}
  \begin{tabularx}{\textwidth}{XXX}
    \toprule
    Nom de la loi & Racine \textsf{R} (alias) & Paramètres \\
    \midrule
    Transformed beta & \texttt{trbeta} (\texttt{pearson4}) & 
      \texttt{shape1} ($\alpha$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$),  \newline
      \texttt{scale}  ($\theta$), \newline
      \texttt{shape2} ($\gamma$), \newline
      \texttt{shape3} ($\tau$) \\
      \midrule
    Generalized Pareto & \texttt{genpareto} & 
      \texttt{shape1} ($\alpha$),  \newline
      \texttt{shape2} ($\tau$),  \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Burr & \texttt{burr} & 
      \texttt{shape1} ($\alpha$), \newline
      \texttt{shape2} ($\gamma$), \newline
      \texttt{rate}   ($\lambda = 1/\theta)$, \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Inverse Burr & \texttt{invburr} & 
      \texttt{shape1} ($\tau$), \newline
      \texttt{shape2} ($\gamma$), \newline
      \texttt{rate}   ($\lambda = 1/\theta)$, \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Pareto & \texttt{pareto} (\texttt{pareto2}) & 
      \texttt{shape}  ($\alpha$), \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Inverse Pareto & \texttt{invpareto} & 
      \texttt{shape}  ($\tau$), \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Loglogistic & \texttt{llogis} & 
      \texttt{shape}  ($\gamma$), \newline
      \texttt{rate}   ($\lambda = 1/\theta)$, \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Paralogistic & \texttt{paralogis} & 
      \texttt{shape}  ($\alpha$), \newline
      \texttt{rate}   ($\lambda = 1/\theta)$, \newline
      \texttt{scale}  ($\theta$) \\
    \bottomrule
  \end{tabularx}
\end{center}

\section*{Famille gamma transformée}

\begin{center}
  \begin{tabularx}{\textwidth}{XXX}
    \toprule
    Nom de la loi & Racine \textsf{R} (alias) & Paramètres \\
    \midrule
    Transformed gamma & \texttt{trgamma} (\texttt{gengamma}) & 
      \texttt{shape1} ($\alpha$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$), \newline
      \texttt{shape2} ($\tau$) \\
      \midrule
    Inverse transformed gamma & \texttt{invtrgamma} (\texttt{igengamma}) & 
      \texttt{shape1} ($\alpha$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$), \newline
      \texttt{shape2} ($\tau$) \\
      \midrule
    Inverse gamma & \texttt{invgamma} & 
      \texttt{shape}  ($\alpha$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Inverse Weibull & \texttt{invweibull} (\texttt{lgompertz}) & 
      \texttt{shape}  ($\tau$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Inverse exponential & \texttt{invexp} & 
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$) \\
    \bottomrule
  \end{tabularx}
\end{center}


\section*{Autres distributions}

\begin{center}
  \begin{tabularx}{\textwidth}{XXX}
    \toprule
    Nom de la loi & Racine \textsf{R} (alias) & Paramètres \\
    \midrule
    Inverse Gaussian\footnote{Laisser pour plus tard.} & \texttt{igauss} & 
      \texttt{mean}   ($\mu$), \newline
      \texttt{shape}  ($\theta$) \\
      \midrule
    Loggamma\footnote{Distribution de $e^X$, où $X \sim
      \mathrm{Gamma}(\alpha, \lambda)$.} & \texttt{lgamma} &
      \texttt{shapelog} ($\alpha$), \newline
      \texttt{ratelog}  ($\lambda$) \\
      \midrule
    Single parameter Pareto & \texttt{pareto1} & 
      \texttt{shape}  ($\alpha$), \newline
      \texttt{min}    ($\theta$) \\
      \midrule
    Inverse gamma & \texttt{igamma} & 
      \texttt{shape}  ($\alpha$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Inverse Weibull & \texttt{iweibull} (\texttt{lgompertz}) & 
      \texttt{shape}  ($\tau$), \newline
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$) \\
      \midrule
    Inverse exponential & \texttt{iexp} & 
      \texttt{rate}   ($\lambda = 1/\theta$), \newline
      \texttt{scale}  ($\theta$) \\
    \bottomrule
  \end{tabularx}
\end{center}

\end{document}

%%% Local Variables: 
%%% mode: latex
%%% TeX-master: t
%%% End: 
