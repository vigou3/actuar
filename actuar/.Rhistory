modelfreq <- list(dist1 = "pois",
                  par1 = list(lambda = quote(Lambda * weights)),
                  dist2 = "gamma",
                  par2 = c(shape = 2, rate = 1))
modelfreq
modelsev<-list(dist1 = "lnorm",
               par1 = list(meanlog = quote(Theta), sdlog = 1),
               dist2 = "norm",
               par2 = c(mean = 5, sd = 1))
simpf(5, 12, modelfreq, modelsev, weights)
weights <- mat(1:60,5,12)
weights <- matrix(1:60,5,12)
simpf(5, 12, modelfreq, modelsev, weights)
print.simpf <- function(x,...)
{
    sapply(paste("Year ",
                 format(1:years, justify = "right"),
                 ":",
                 format(x[1,1:years])),
           cat,
           fill=TRUE)
}
simpf(5, 12, modelfreq, modelsev, weights)
simpf(5, 12, modelfreq, modelsev, weights)
simpf(5, 12, modelfreq, modelsev, weights)
simpf(5, 12, modelfreq, modelsev, weights)
simpf(5, 12, modelfreq, modelsev, weights)
simpf(5, 12, modelfreq, modelsev, weights)
simpf(5, 12, modelfreq, modelsev, weights)
q()
n
